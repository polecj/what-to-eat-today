# 提交记录

> 记录每次 commit & push 的更新内容。版号规则：格式 `MMDDNN`，`MM` 月份，`DD` 日期，`NN` 当天序号。

## 早期（版号制度前）

- first commit
- 添加 README，修复若干问题
- 配置 GitHub Pages 部署
- 新增分享码生成与导入功能
- 修复删除条目时列表展开动画问题
- 添加 .gitignore

## 061001 →
- 教学模式禁用分享码按钮，新增分享码教学步骤
- 分享码前缀 `WTE:` 大小写不敏感
- 新增备份提示，移动端支持滚动
- 修正备份提示位置
- 修复移动端教学模式布局
- 完善移动端标签和教学交互
- 修复教学导入和移动端拖动

## 061016
- 审查随机算法，数学上公平无偏
- 新增抽取统计：每项显示被抽中次数，大类标题显示类内合计
- 汉堡菜单新增「还原统计」按钮，点击归零并 toast 告知
- 修复列表展开动画误触发，引入 `categoriesToAnimate` 精确控制

## 061017
- 分享码编码改为行式（一行一个项目），省去 JSON 结构开销，长度约减半
- 解码兼容新旧两种格式

## 061021
- 合并导入新增类别和新增条目显示黄色高亮，动画延长为先保持再渐隐
- 保持类别原有展开状态，未展开类别不强制展开

## 061101
- 导入确认弹窗新增合并预览，显示新增、重复和涉及类别
- 导入完成后 toast 提示导入统计结果
- 合并导入新增项目在首次抽取前显示“新增”标记，首次抽取后消失
- 整理 TODO，补充统计页后续计划

## 061102
- 教学第 0 步返回直接退出，不再弹出确认
- 待抽列表标题改为”你的心头好”
- 汉堡菜单新增普通模式抽取时间设置，支持 1.0s 到 8.0s、精确到 0.1s
- 普通模式抽取动画按设置时长播放，默认 2.5s

## 08（061103-062307）
- 抽取时间设置改为加减按钮，下限 0.3s
- 条目右侧 ⋯ 弹出菜单，含编辑、抽出权重、删除
- 抽出权重：更少出现/正常/更多出现三档，加权随机
- 取消标题栏全收、抽取后自动展开/收缩
- 抽取统计页面：抽中/就餐双柱状图，可切换重叠对比
- “今天我吃了……”：类别→项目联动选择，记录就餐次数
- 条目行显示抽中/吃过次数，统计图同步
- 类别输入框下拉箭头，从已有类别选择
- 搜索框缩短，统计入口移入抽取历史框
- 修复汉堡菜单、列表渲染、标签抽取等多个 bug

## 09（062308）
- 禁用 iOS Safari 输入框和双击自动缩放
- 还原统计和覆盖导入增加确认弹窗

## 10（062309-062311）
- 移动端滚动调整，取消自动滚动只保留高亮

## 11（062312-062316）
- 欢迎页进入动画加长到 700ms，两种模式切换动画加长
- 修复标签抽取时部分标签盖住抽取结果（`.tag-board` 加 `isolation` 收敛层叠上下文）
- “今天我吃了……”的两级选择由原生 select 换成与类别输入框一致的自定义下拉列表
- 教学第 1、2 步输入框改为只读并关闭设备键盘，点击弹出建议选项
- 修复教学第 1、2 步的输入内容退出后残留在主页
- 教学第 7 步禁用「覆盖导入」「取消」，只保留合并导入
- 教学第 8 步生成分享码复用主页面弹窗，提示不生成分享码
- 教学第 9 步「下一步」与「返回」同步闪烁
- 教学期间用不到的控件统一置灰禁用，导入/导出 JSON 不产生真实副作用
- 删除「批量编辑」功能（多类别时无后续步骤，是条死路）

## 12（062317-062322）
- 欢迎页进入动画还原原版时长，全站禁用双击放大
- 教学第 12 步不再唤起键盘；第 9 步「下一步」「返回」同步闪烁
- 教学期间用不到的控件统一置灰，导入/导出 JSON 不产生真实副作用
- 「还原统计」「覆盖导入」改用自定义确认弹窗，并各自增加第二层二次确认
- 统计页新增排序：表头箭头按抽取次数/就餐次数从高到低排，列被隐藏时自动让位
- 「统计」入口移入 ☰ 菜单，「还原统计」移入统计页底部并加红色底色
- 排序箭头改成 34×26 按钮，选中绿色、未选中灰白，不进 Tab 顺序
- 修复就餐次数柱子滚动时糊到表头、以及排序箭头被全局 button.pick 接管的问题
